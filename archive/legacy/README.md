# Archived scripts

The files in this directory are the original standalone scripts and the
unrelated `ExportifyX` prototype. They are retained for reference while the
packaged `spotm3u` application is built under `src/spotm3u`.

They are not part of the installed CLI and should not be used as the entry
point for new work.
